<div class="portfolio-shell">
	<div class="ambient ambient-left"></div>
	<div class="ambient ambient-right"></div>

	<section class="portfolio-card">
		<div class="eyebrow">Portafolio</div>
		<h1>Leonardo Ramirez</h1>
		<p class="lead">
			Desarrollador de Software apasionado por las tecnologias 4.0.
		</p>

		<div class="about">
			<p>
				Construyo experiencias web con foco en interfaces claras, codigo mantenible y
				soluciones practicas.
			</p>
		</div>

		<div class="skills-block">
			<h2>Skills</h2>
			<ul class="skills-list">
				<li>JavaScript</li>
				<li>TypeScript</li>
				<li>HTML</li>
				<li>CSS3</li>
				<li>Tailwind</li>
				<li>MongoDB</li>
				<li>MySQL</li>
			</ul>
		</div>

		<div class="links-block">
			<a
				class="primary-link"
				href="https://www.linkedin.com/in/jleonardoramirez/"
				target="_blank"
				rel="noreferrer"
			>
				LinkedIn
			</a>
			<a
				class="secondary-link"
				href="https://github.com/LeoRami-99"
				target="_blank"
				rel="noreferrer"
			>
				GitHub
			</a>
		</div>
	</section>
</div>

<style>
	:root {
		--bg-main: #f4efe7;
		--bg-card: rgba(255, 250, 242, 0.76);
		--text-main: #1f2937;
		--text-soft: #4b5563;
		--accent: #c26a2d;
		--accent-dark: #8d4720;
		--line: rgba(96, 66, 44, 0.14);
		--shadow: 0 30px 80px rgba(89, 59, 34, 0.18);
	}

	.portfolio-shell {
		position: relative;
		min-height: 100vh;
		display: grid;
		place-items: center;
		padding: 32px 20px;
		overflow: hidden;
		background:
			radial-gradient(circle at top left, rgba(236, 174, 112, 0.28), transparent 30%),
			radial-gradient(circle at bottom right, rgba(91, 141, 122, 0.18), transparent 34%),
			linear-gradient(135deg, #f8f3ec 0%, #efe4d3 48%, #e6d7c1 100%);
		font-family: Georgia, "Times New Roman", serif;
	}

	.ambient {
		position: absolute;
		border-radius: 999px;
		filter: blur(10px);
		opacity: 0.6;
		pointer-events: none;
		animation: drift 9s ease-in-out infinite;
	}

	.ambient-left {
		top: 10%;
		left: -40px;
		width: 180px;
		height: 180px;
		background: rgba(194, 106, 45, 0.18);
	}

	.ambient-right {
		right: -30px;
		bottom: 12%;
		width: 220px;
		height: 220px;
		background: rgba(92, 134, 118, 0.18);
		animation-delay: 1.4s;
	}

	.portfolio-card {
		position: relative;
		width: min(100%, 760px);
		padding: 40px;
		border: 1px solid var(--line);
		border-radius: 28px;
		background: var(--bg-card);
		backdrop-filter: blur(16px);
		box-shadow: var(--shadow);
		color: var(--text-main);
		animation: rise 0.8s ease-out;
	}

	.eyebrow {
		display: inline-flex;
		padding: 8px 14px;
		border-radius: 999px;
		background: rgba(194, 106, 45, 0.12);
		color: var(--accent-dark);
		font-size: 12px;
		letter-spacing: 0.18em;
		text-transform: uppercase;
	}

	h1 {
		margin: 18px 0 10px;
		font-size: clamp(2.5rem, 8vw, 4.6rem);
		line-height: 0.95;
		letter-spacing: -0.05em;
	}

	.lead {
		max-width: 30rem;
		margin: 0;
		font-size: 1.15rem;
		line-height: 1.7;
		color: var(--text-soft);
	}

	.about {
		margin-top: 28px;
		padding-top: 24px;
		border-top: 1px solid var(--line);
	}

	.about p {
		margin: 0;
		max-width: 34rem;
		font-family: "Trebuchet MS", "Segoe UI", sans-serif;
		font-size: 1rem;
		line-height: 1.8;
		color: var(--text-soft);
	}

	.skills-block {
		margin-top: 30px;
	}

	h2 {
		margin: 0 0 16px;
		font-family: "Trebuchet MS", "Segoe UI", sans-serif;
		font-size: 0.95rem;
		letter-spacing: 0.08em;
		text-transform: uppercase;
		color: var(--accent-dark);
	}

	.skills-list {
		display: flex;
		flex-wrap: wrap;
		gap: 12px;
		padding: 0;
		margin: 0;
		list-style: none;
	}

	.skills-list li {
		padding: 10px 14px;
		border: 1px solid rgba(141, 71, 32, 0.16);
		border-radius: 999px;
		background: rgba(255, 255, 255, 0.5);
		font-family: "Trebuchet MS", "Segoe UI", sans-serif;
		font-size: 0.95rem;
		color: var(--text-main);
	}

	.links-block {
		display: flex;
		flex-wrap: wrap;
		gap: 14px;
		margin-top: 34px;
	}

	.links-block a {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		min-width: 140px;
		padding: 14px 18px;
		border-radius: 14px;
		text-decoration: none;
		font-family: "Trebuchet MS", "Segoe UI", sans-serif;
		font-weight: 700;
		transition:
			transform 0.2s ease,
			box-shadow 0.2s ease,
			background 0.2s ease;
	}

	.primary-link {
		background: linear-gradient(135deg, #c26a2d 0%, #8d4720 100%);
		color: #fff8f1;
		box-shadow: 0 18px 35px rgba(141, 71, 32, 0.22);
	}

	.secondary-link {
		border: 1px solid rgba(141, 71, 32, 0.2);
		background: rgba(255, 255, 255, 0.56);
		color: var(--text-main);
	}

	.links-block a:hover {
		transform: translateY(-2px);
	}

	.primary-link:hover {
		box-shadow: 0 22px 42px rgba(141, 71, 32, 0.28);
	}

	.secondary-link:hover {
		background: rgba(255, 255, 255, 0.8);
	}

	@keyframes rise {
		from {
			opacity: 0;
			transform: translateY(22px);
		}

		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	@keyframes drift {
		0%,
		100% {
			transform: translateY(0px);
		}

		50% {
			transform: translateY(-14px);
		}
	}

	@media (max-width: 640px) {
		.portfolio-shell {
			padding: 20px 16px;
		}

		.portfolio-card {
			padding: 28px 22px;
			border-radius: 22px;
		}

		.links-block a {
			width: 100%;
		}
	}
</style>
